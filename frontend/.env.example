# Leave blank locally to use Vite's /api proxy. Set this in Vercel to the Render URL.
VITE_API_URL=
# Legacy name still supported by src/api.js.
# VITE_API_BASE_URL=

# OAuth client IDs are public browser configuration, not client secrets.
VITE_GOOGLE_CLIENT_ID=
